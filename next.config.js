/** @type {import('next').NextConfig} */
const nextConfig = {
  // ここに設定を書く
  reactStrictMode: true,
  // その他の設定...
  compiler: {
    // styledComponents有効化
    styledComponents: true,
  },
};

module.exports = nextConfig;
